---
import { Icon } from "astro-icon/components"

import { cn } from "@/lib/utils"

export interface BreadcrumbItem {
  href?: string
  label: string
  icon?: string
}

interface Props {
  items: BreadcrumbItem[]
  class?: string
}

const { items, class: className } = Astro.props
---

<nav aria-label="breadcrumb" class={cn("", className)}>
  <ol
    class="text-muted-foreground flex flex-wrap items-center gap-1.5 text-sm break-words sm:gap-2.5"
  >
    <li class="inline-flex items-center gap-1.5">
      <a href="/" class="hover:text-foreground flex items-center transition-colors">
        <Icon name="mingcute:home-4-line" class="size-5 shrink-0" />
      </a>
    </li>

    {
      items.map((item, index) => (
        <>
          <li role="presentation" aria-hidden="true">
            <Icon name="mingcute:right-line" class="size-4" />
          </li>
          <li class="inline-flex items-center gap-1.5">
            {index === items.length - 1 ? (
              <span
                role="link"
                aria-disabled="true"
                aria-current="page"
                class="text-foreground flex items-center font-normal"
              >
                {item.icon && <Icon name={item.icon} class="mr-1 size-5 shrink-0" />}
                {item.label}
              </span>
            ) : (
              <a href={item.href} class="hover:text-foreground flex items-center transition-colors">
                {item.icon && <Icon name={item.icon} class="mr-1 size-5 shrink-0" />}
                <span class="leading-none">{item.label}</span>
              </a>
            )}
          </li>
        </>
      ))
    }
  </ol>
</nav>
